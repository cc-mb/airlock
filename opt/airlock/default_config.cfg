{
  pns = {
    enabled = true,
    prefix = nil
  },
  chamber = {
    decontamination = {
      device = "chamber.decon",
      device_side = "back",
      duration = 10,
      direction = "bidirectional"
    },
    panel = {
      device = "chamber.panel",
      name = nil
    }
  },
  left = {
    door = {
      device = "left.door",
      device_side = "back",
      keep_open_duration = 2,
      transition_duration = 1
    },
    lock = {
      device = "left.lock",
      device_side = "back",
      level = nil,
      unlock_duration = 3
    },
    panel = {
      device = "left.panel",
      room = {
        hazard = nil,
        name = nil,
        number = nil
      }
    }
  },
  right = {
    door = {
      device = "right.door",
      device_side = "back",
      keep_open_duration = 2,
      transition_duration = 1
    },
    lock = {
      device = "right.lock",
      device_side = "back",
      level = nil,
      unlock_duration = 3
    },
    panel = {
      device = "right.panel",
      room = {
        hazard = nil,
        name = nil,
        number = nil
      }
    }
  }
}