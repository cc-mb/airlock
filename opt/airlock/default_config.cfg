{
  chamber = {
    decontamination = {
      device = nil, -- mandatory
      device_side = "back",
      duration = 10,
      direction = "in-out"
    },
    panel = {
      device = nil, -- mandatory
      name = nil
    }
  },
  inner = {
    door = {
      device = nil, -- mandatory
      device_side = "back",
      keep_open_duration = 2,
      transition_duration = 1
    },
    lock = {
      device = nil,
      device_side = "back",
      level = nil,
      unlock_duration = 3
    },
    panel = {
      device = nil, -- mandatory
      room = {
        hazard = nil,
        name = nil,
        number = nil
      }
    }
  },
  outer = {
    door = {
      device = nil, -- mandatory
      device_side = "back",
      keep_open_duration = 2,
      transition_duration = 1
    },
    lock = {
      device = nil,
      device_side = "back",
      level = nil,
      unlock_duration = 3
    },
    panel = {
      device = nil, -- mandatory
      room = {
        hazard = nil,
        name = nil,
        number = nil
      }
    }
  }
}